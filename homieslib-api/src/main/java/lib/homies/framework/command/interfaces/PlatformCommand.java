package lib.homies.framework.command.interfaces;

/**
 * A marker interface for platform-specific command implementations.
 * This interface is used to denote that a class is a command that interacts
 * directly with the underlying platform's command system.
 */
public interface PlatformCommand {
}
